{
  "name": "api-postman-newman",
  "private": true,
  "scripts": {
    "test": "npm run api:test:mock",
    "api:test:mock": "concurrently -k -s first \"npm run mock:server\" \"npm run mock:wait-and-test\"",
    "api:test:real": "newman run collection.json -e env.json -r cli,htmlextra --reporter-htmlextra-export newman.html",
    "mock:server": "node mock-server.js",
    "mock:wait-and-test": "wait-on http://localhost:4010/api/users && newman run collection.json -e env-mock.json -r cli,htmlextra --reporter-htmlextra-export newman.html"
  },
  "devDependencies": {
    "newman": "^6.1.0",
    "newman-reporter-htmlextra": "1.23.1",
    "json-server": "0.17.4",
    "concurrently": "8.2.2",
    "wait-on": "7.2.0"
  }
}
