# Detect text files and normalize to LF in the repo by default
* text=auto eol=lf

# Windows-only scripts keep CRLF when checked out on Windows
*.ps1 text eol=crlf
*.bat text eol=crlf

# Things executed/parsed on Linux CI must be LF
*.sh text eol=lf
*.bash text eol=lf
*.yml text eol=lf
*.yaml text eol=lf
Jenkinsfile text eol=lf
Dockerfile text eol=lf
